import { ChangeUserProfile } from '@/features';
import { Container } from '@/shared';

export const UserProfileUpdateWidget = () => {
  return (
    <Container>
      <ChangeUserProfile />
    </Container>
  );
};
